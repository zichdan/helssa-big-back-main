"""
تنظیمات اپلیکیشن احراز هویت OTP
OTP Authentication App Configuration
"""

from django.apps import AppConfig


class AuthOtpConfig(AppConfig):
    """
    پیکربندی اپلیکیشن auth_otp
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'auth_otp'
    verbose_name = 'احراز هویت OTP'
    
    def ready(self):
        """
        عملیات اولیه هنگام بارگذاری اپلیکیشن را انجام می‌دهد.
        
        این متد پس از آماده‌شدن رجیستری اپلیکیشن فراخوانی می‌شود و نقطهٔ مناسب برای انجام تنظیمات مرتبط با چرخهٔ اجرای اپ (مانند واردکردن هندلرهای سیگنال، ثبت validation checks یا راه‌اندازی اجزای مرتبط با اپ) است. معمولاً در اینجا:
        - هندلرهای سیگنال را وارد کنید (مثلاً `import auth_otp.signals`) تا اتصالات سیگنال برقرار شوند.
        - ثبت یا پیکربندی تک‌باره‌ای که نیاز به دسترسی به مدل‌ها یا رجیستری اپ دارد انجام شود.
        
        تذکرها:
        - این متد یک‌بار در طول چرخهٔ فرآیند اجرا می‌شود؛ از انجام کارهای زمان‌بر یا مسدودکننده در آن خودداری کنید.
        - از ایجاد اثرات جانبی غیرقابل پیش‌بینی که مانع راه‌اندازی صحیح اپ می‌شود پرهیز کنید.
        """
        # می‌توانید سیگنال‌ها را اینجا import کنید
        # import auth_otp.signals
        pass